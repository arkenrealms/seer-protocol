# arken/packages/seer/packages/protocol/.rush/README.md

## Purpose
Holds Rush-managed transient install metadata for `@arken/seer-protocol`.

## Notes
- `temp/` contains generated dependency lock snapshots used by Rush tooling.
- Treat this directory as generated state, not a hand-edited source surface.
- If lock metadata needs to change, regenerate through Rush workflows rather than direct edits.
